// TODO(week 7): Playwright/CLI entry. Launches a headless Chromium with
//   --enable-unsafe-webgpu --use-angle=metal/vulkan and bench.html, drives the
//   bench composition root via window.__zizi_bench__, collects BenchRecord JSON,
//   writes to ./bench-results/<scenario>-<sha>-<timestamp>.json.
//
// For now this is a placeholder so the directory is part of the repo shape.
//
// Usage (planned):
//   pnpm tsx src/bench/scripts/run.ts --scenario orbit-360
export {};
